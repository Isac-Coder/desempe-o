{
  "name": "simulacro-desempeno",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "json-server": "json-server --watch db.json --port 3000",
    "start": "concurrently \"npm run json-server\" \"npm run dev\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "json-server": "^0.17.3",
    "vite": "^5.4.1"
  }
}
